# Ping Smoke Test

Tiny illustrative example for verifying that a Diamond project manifest, prompt block, workspace variable, and pipeline all line up.

Use this when you want the smallest possible working pattern:
- one workspace variable
- one prompt block
- one pipeline
- one generated output file

This is an example bundle to adapt, not a scriptless promise that any repo can run it unchanged.
